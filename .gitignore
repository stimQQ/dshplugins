node_modules/
.DS_Store
*.log

# Build output
.next/
out/
*.tsbuildinfo

# Vercel project link (machine-local)
.vercel/

# Downloaded tarballs — reproducible, and large
packages/registry/.cache/

# Pipeline intermediates. crawl.ts and verify.ts regenerate these on every run, and
# nothing but the next stage reads them; committing 8.7 MB of churn daily buys nothing.
# The stage-3 outputs below them ARE committed, because the site builds from those.
packages/registry/data/raw-npm.json
packages/registry/data/verified.json

# Generated at build time from packages/registry/data
apps/web/public/data/
apps/web/public/api/
